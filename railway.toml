[service]
name = "heiwa-cloud-hq"

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/swarm-hub/Dockerfile"

[deploy]
startCommand = "bash apps/swarm-hub/start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 30
